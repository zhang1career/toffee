import type { ReactNode } from "react";

/** Reserved for a persistent top banner strip (not implemented). */
export function TopBannerPlaceholder(_props: { children?: ReactNode }): null {
  return null;
}

/** Reserved for modal dialogs (not implemented). */
export function ModalPlaceholder(_props: { children?: ReactNode }): null {
  return null;
}
